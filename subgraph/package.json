{
  "name": "erlc-subgraph",
  "version": "1.0.0",
  "author": "",
  "license": "ISC",
  "description": "",
  "main": "subgraph.yaml",
  "scripts": {
    "build:schema": "concat -o schema.graphql node_modules/@amxx/graphprotocol-utils/schema.graphql src/schema.gql",
    "build:codegen": "graph codegen",
    "build:compile": "graph build",
    "build": "yarn build:schema && yarn build:codegen && yarn build:compile"
  },
  "dependencies": {
    "@iexec/erlc": "0.0.5",
    "rlc-faucet-contract": "^1.0.9"
  },
  "devDependencies": {
    "@amxx/graphprotocol-utils": "0.0.6-2",
    "@graphprotocol/graph-cli": "^0.19.0",
    "@graphprotocol/graph-ts": "^0.19.0",
    "concat": "^1.0.3"
  }
}
