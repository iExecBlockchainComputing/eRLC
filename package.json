{
  "name": "KERC20",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Hadrien Croubois (@Amxx)",
  "description": "",
  "scripts": {
    "clean": "rimraf build dist",
    "build:solidity": "oz compile",
    "build:ts": "tsc --build tsconfig.json",
    "build": "yarn build:solidity && yarn build:ts",
    "lint": "solhint --max-warnings 0 \"contracts/**/*.sol\"",
    "test": "mocha 'test/*.spec.js' --exit --recursive --timeout 10000"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.2.1-solc-0.7"
  },
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/test-environment": "^0.1.5",
    "@openzeppelin/test-helpers": "^0.5.6",
    "chai": "^4.2.0",
    "ethers": "^5.0.19",
    "ganache-cli": "^6.12.0",
    "loadsh": "0.0.4",
    "mocha": "^8.1.3",
    "readline": "^1.3.0",
    "rimraf": "^3.0.2",
    "rlc-faucet-contract": "^1.0.9",
    "solhint": "^3.2.1",
    "typescript": "^4.0.5"
  }
}
